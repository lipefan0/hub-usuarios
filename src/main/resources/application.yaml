spring:
  application:
    name: hub-usuarios

  datasource:
    url: jdbc:postgresql://localhost:5432/db_usuario
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect